# Optional: OpenAI API key for enhanced answer generation
# If not provided, the system will still work but will return basic search results
OPENAI_API_KEY=your_openai_api_key_here

# Flask secret key
SECRET_KEY=your_secret_key_here